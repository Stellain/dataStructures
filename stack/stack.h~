#include<iostream>
#include<string>
using namespace std;
class stack{
	private:
		int a[10];
		int top;
	public:
		static const string inf="DONE BY STELLA";
		void stackPush(int );
		int stackPop();
		bool isEmpty();
		int stackTop();
		stack();
};
